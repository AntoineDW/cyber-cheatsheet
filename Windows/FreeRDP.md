# FreeRDP

FreeRDP is a tool used to access a Windows computer remotely using the Remote Desktop Protocol.

## Access a Windows computer remotely
```bash
xfreerdp -u:[user] -p:[password] -v:[ip] -d:[domain]
```
