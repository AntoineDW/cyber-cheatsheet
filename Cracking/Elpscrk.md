# Elpscrk

Elpscrk (Elliot Password Cracker) is a password generator using the target's personal informations.

## Generate a password list
```bash
python3 /opt/elpscrk/elpscrk.py --level [level] --min [min] --max [max] -x [output]
```
